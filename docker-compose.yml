services:
  app:
    build: .
    container_name: colecifebens-app
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: sh -c "if [ ! -f .env ]; then cp .env.example .env; fi && php artisan key:generate && php artisan serve --host=0.0.0.0 --port=8000" 
    # depends_on:
    # - db

#   db:
#     image: mysql:8
#     container_name: colecifebens-db
#     restart: always
#     environment:
#       MYSQL_ROOT_PASSWORD: root
#       MYSQL_DATABASE: colecifebens
#     ports:
#       - "3306:3306"
#     volumes:
#       - db_data:/var/lib/mysql

# volumes:
#   db_data: